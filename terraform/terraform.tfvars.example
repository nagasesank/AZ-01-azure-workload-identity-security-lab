# Phase 2 requires only a deployment location.
# Local authentication uses the active Azure CLI session from `az login`.
# ARM_SUBSCRIPTION_ID and ARM_TENANT_ID must match that active account.
# Do not place real identifiers or credentials in this example or any committed tfvars file.

# location = "eastus"
